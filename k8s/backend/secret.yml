apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: fitoglobe

type: Opaque

stringData:
  DATABASE_URL: "postgresql://fitoglobe:fitoglobe@postgres:5432/fitoglobe"
  DIRECT_URL: "postgresql://fitoglobe:fitoglobe@postgres:5432/fitoglobe"
  JWT_SECRET: "REPLACE_ME"
  JWT_EXPIRES_IN: "7d"